---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
#  - name: ubuntu-12.04
  - name: centos-6.7

suites:
  - name: default
    run_list:
      - recipe[role_sinatra_app::default]
    attributes:
